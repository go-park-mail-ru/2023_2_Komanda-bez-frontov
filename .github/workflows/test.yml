name: test
on:
  push:
    branches:
      - master
      - main
      - dev
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: golang:1.21-alpine

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Run tests
      run: make test
